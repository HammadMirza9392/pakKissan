import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PaymentClearence = ({navigation}) => {
  return (
    <View>
      <Text>PaymentClearence</Text>
    </View>
  );
};

export default PaymentClearence;

const styles = StyleSheet.create({});
